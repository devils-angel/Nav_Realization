<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nav_realization">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".lotus" android:label="LOTUS" android:parentActivityName=".MainActivity"> <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity"/></activity>
        <activity android:name=".lavender" android:label="LAVENDER" android:parentActivityName=".MainActivity"/>
        <activity android:name=".poppy" android:label="POPPY" android:parentActivityName=".MainActivity"/>
        <activity android:name=".lily" android:label="LILY" android:parentActivityName=".MainActivity" />
        <activity android:name=".rose" android:label="ROSE" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>